# Copy this file to `.env.local` or `.env` and fill in your Supabase credentials.
# Do NOT commit .env.local with real keys to version control.

# Supabase project URL (found in Supabase dashboard)
VITE_SUPABASE_URL=https://your-project-ref.supabase.co

# Supabase publishable (anon) key — safe for client-side usage
VITE_SUPABASE_PUBLISHABLE_KEY=eyJhbGciOiJIUzI1NiIsInR5cCI6IkpXVCJ9...your_anon_key...

# If you need to use a service_role key (server-side), do NOT prefix with VITE_ and keep it secret.
# SERVICE_ROLE_KEY=your_service_role_key

# Server: optional - service role key used by server-side API endpoints. Keep this secret and DO NOT commit.
SERVICE_ROLE_KEY=your_service_role_key_here
